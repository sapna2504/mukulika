{
  "version": 2,
  "builds": [
    {
      "src": "vercel-build.sh",
      "use": "@vercel/static-build",
      "config": { "distDir": "_site" }
    }
  ],
  "outputDirectory": "_site",
  "routes": [
    { "src": "/(.*)", "dest": "/_site/$1" }
  ]
}
